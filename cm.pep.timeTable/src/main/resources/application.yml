  spring:
    application:
      name: even

    datasource:
      url: jdbc:postgresql://localhost:5432/db_time_table
      username: postgres
      password: postgres
      driver-class-name: org.postgresql.Driver

    jpa:
      open-in-view: false
      hibernate:
        ddl-auto: update
      show-sql: true
